// Combine a Wiggle and a Loop on one Parameter

// you can also put "pingpong" instead of "cycle" for your loop
L = loopOut ("cycle" ) ; 

// 3 is the times per second, 30 is the number of pixels it will vacillate
W = wiggle ( 3, 30 ) ; 

L + W - value;
